// Imported via the esbuild text loader as a string (see the loader config in esbuild.config.mjs).
import raw from "./default-template.md";

/** Full text of the built-in default general template (5phase-lite). */
export const DEFAULT_BUILTIN_TEMPLATE: string = raw;
